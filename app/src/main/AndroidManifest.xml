<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.brus5.lukaszkrawczak.fitx">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/fitx"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".UserLoginActivity"
            android:label="@string/fitx"
            android:theme="@style/FiTX3theme.noBar"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="FiTX - Gym Tracker"
            android:theme="@style/FiTX3theme.noBar" />
        <activity android:name=".UserRegisterActivity" />
        <activity android:name=".UserProfileActivitybackup" />
        <activity
            android:name=".UserProfileActivity"
            android:theme="@style/FiTX3theme.noBar"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".TrainingActivity"
            android:label="Training"
            android:theme="@style/FiTX3theme.noBar" />
        <activity
            android:name=".DietActivity"
            android:label="Diet"
            android:theme="@style/FiTX3theme.noBar" />
        <activity android:name=".MetacalcActivity" />
        <activity android:name=".MetacalcActivity2" />
        <activity
            android:name=".MetacalcActivity3"
            android:label="Calorie Calculator"
            android:theme="@style/FiTX3theme.noBar" />
        <activity
            android:name=".Metacalc.MetacalcGymActivity"
            android:label="Add gym training"
            android:theme="@style/FiTX3theme.noBar" />
        <activity
            android:name=".Metacalc.MetacalcAreoActivity"
            android:label="Add cardio training"
            android:theme="@style/FiTX3theme.noBar" />
        <activity
            android:name=".DietSearchActivity"
            android:label="Search for product"
            android:theme="@style/FiTX3theme.noBar">

            <!-- android:theme="@style/FiTX3theme.noBar" -->

        </activity>
        <activity
            android:name=".TrainingSearchActivity2"
            android:label="Search for training"
            android:theme="@style/FiTX3theme.noBar">

            <!-- android:theme="@style/FiTX3theme.noBar" -->

        </activity>

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name=".Main2Activity"
            android:label="@string/fitx"
            android:theme="@style/FiTX3theme.noBar" />
        <activity
            android:name=".DietSearchActivity2"
            android:label="Search for product"
            android:theme="@style/FiTX3theme.noBar" />
        <activity
            android:name=".UserRegisterActivityFirstPage"
            android:label="FiTX/Registration"
            android:theme="@style/FiTX3theme.noBar" />
        <activity
            android:name=".UserRegisterActivitySecondPage"
            android:label="FiTX/Registration"
            android:theme="@style/FiTX3theme.noBar" />
        <activity
            android:name=".UserRegisterActivityThirdPage"
            android:label="FiTX/Registration"
            android:theme="@style/FiTX3theme.noBar" >

        </activity>
    </application>

</manifest>